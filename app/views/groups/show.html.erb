<h3><%= image_tag @group.avatar.url(:thumb) %><%= @group.name %></h3>
<h4><%= @group.description %></h4>
<% if @group.members.include?(current_user) %>
	<% @group.tags.each do |tag|%>
		<%= tag.name %>
	<% end %>
	<h4>Members</h4>

		<% if @group.members %>
			<ul>
			  <% @group.members.each do |member| %>
			    <li>
				    <!-- <a href="/users/<%#=member.id%>" class="group-members"%><%#=image_tag member.avatar(:thumb)%><%#=member.name%></a> -->
				    <%=image_tag member.avatar(:thumb)%><%=member.name%>
				    <% if (current_user == @group.creator) && (member != @group.creator) %>
				    	<button class="destroy">Remove</button>
				    <% end %>
			    </li>
			  <% end %>
			 </ul>
	  <% end %>

<!-- 	<h5>Search for Friends and Add Them to this Group</h5>
		<%#= form_tag('/users/search', :method => "get", id: "search-form") do %>
		<%#= text_field_tag :search, params[:search], placeholder: "Search Users" %>
		<%#= hidden_field_tag :group_id, params[:id] %>
		<%#= submit_tag "Search", :name => nil %>
		<%# end %> -->
<% if flash[:notice] %>
  <div class="notice"><%= flash[:notice] %></div>
<% end %> 

 <div class="search">
 <%= form_tag :controller => 'search', :action => 'create', :method => 'get' do %>
      <%= text_field_tag :keyword, params[:keyword], :id => 'search_field', placeholder: "Connect with others!" %>
      <%= hidden_field_tag :group_id, @group.id %>
      <%= submit_tag "Search ", :content => nil %> 
  <% end %> 
</div>

<%= render 'search/results' if @results %>

	<%# if @search_results %>
		<%# render 'users/search_results' %>
	<%# end %>

	<br>

		<%= render 'boards/form' %><br>
	<h2>Groups Boards</h2>
		<div id="group-boards">
			<%= render @boards %>
		  <%#= render 'comments/form' %><br>
		  <%#= render @comments %>
		 </div>
<% end %>
