<!-- <%#= li_for_goal(goal) do %>
  <tr>
    <%#= form_for([current_user, goal], url: goals_path) do |f| %>
    <td><%#= f.check_box :status, class: "toggle", "data-id": "#{goal.id}" %></td>
    <td><%#=link_to "#{goal.name}", goal_path(goal) %></td>
    <td><button class="destroy" data-id="<%#=goal.id%>"></button></td>
    <%# end %>
  </tr>
  <%# end %> -->



    <!-- <div class="card-body m-t-0"> -->
<!--         <table class="table table-inner table-vmiddle">
  <thead> -->

    <!-- <tr>
      <th>Status</th>
      <th>Goal</th>
      <th>Delete</th>
    </tr> -->
    <!-- </thead> -->
    <!--     <tbody> -->
    <% if @goals %>
    <% @goals.each do |goal| %>
    <%= form_for([current_user, goal], url: goals_path) do |f| %>
    <tr>
      <td><%= f.check_box :status, class: "toggle", "data-id": "#{goal.id}" %></td>
      <td><%=link_to "#{goal.name}", goal_path(goal) %></td>
      <td><%= goal.due_date%></button></td>
    </tr>
    <% end %>
    <%end%>
    <%end%>
            </<!-- tbody>
          </table> -->
          <!-- </div> -->
  </div>
