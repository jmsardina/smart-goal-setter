<h1><%= current_user.name %> </h1>

<ul>
  <% @goals.each do |goal| %>
    <li> <%=link_to "#{goal.name}", goal_path(goal) %>
      <p> <%= goal.description %></p>
    <li>
  <% end %>
</ul>


<p>This page will be the the user's dashboard.</p>

<ul> This page may include:
  <li>user name</li>
  <li>profile picture</li>
  <li>links to goals</li>
  <li>newsfeed</li>
</ul>
