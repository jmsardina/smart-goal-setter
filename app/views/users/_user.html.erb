<div class="member-block">
  <%= div_for_user(user) do %>
    <!-- <div class="member-picture"> -->
      <% if user.image_url %>
        <img src="<%=user.image_url%>" style="height:auto;width:auto;max-width:50px;max-height:50px;"><br>
      <% else %>
        <img src="<%=user.avatar.url%>" style="height:auto;width:auto;max-width:50px;max-height:50px;"><br>
      <% end %>
    <!-- </div> -->
        <%=user.name %>
    <%# if (user == @group.creator) && (user != @group.creator) %>
      <!-- <button class="destroy">Remove</button> -->
    <%# end %>
  <% end %>
</div>

